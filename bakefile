CC = gcc
CARGS = -std=c99 -Wall -pedantic -Werror
NAME = try
OBJECT = $(NAME).o
SOURCE = $(NAME).c
BINARY = $(NAME).out

$(OBJECT): $(SOURCE)
	$(CC) $(CARGS) -c -o $(OBJECT) $(SOURCE)

$(BINARY): $(OBJECT)
	$(CC) $(CARGS) -o $(BINARY) $(OBJECT)
